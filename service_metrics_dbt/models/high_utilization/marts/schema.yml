version: 2

models:
  - name: high_utilization
    description: "Staged high utilization data — cleaned nulls, trimmed strings"
    columns:
      - name: week
        tests: [not_null]
      - name: date
        tests: [not_null]
      - name: tech
        tests: [not_null]
      - name: vendor
        tests: [not_null]
      - name: site_name
        tests: [not_null]
      - name: cell_name
        tests: [not_null]
      - name: municipality
        tests: [not_null]
      - name: province
        tests: [not_null]
      - name: band
        tests: [not_null]